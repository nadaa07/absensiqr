<?php require_once __DIR__.'/../config/database.php';require_once __DIR__.'/../config/auth.php';require_once __DIR__.'/../config/helpers.php';requireRole('mahasiswa');$base='..';$assetBase='../assets';$pageTitle='Izin / Sakit';require __DIR__.'/../partials/head.php'; ?><div class="dashboard-layout"><?php require __DIR__.'/../partials/sidebar.php'; ?><main class="main"><div class="mobile-head"><b>Sistem Absensi QR</b><button class="menu-toggle">☰</button></div><header class="topbar"><div><h1>Izin / Sakit</h1><p>Ajukan ketidakhadiran untuk sesi yang tersedia.</p></div><div class="top-actions"><a class="btn btn-ghost" href="../index.php">↗ Beranda</a></div></header>
<?php $msg='';if($_SERVER['REQUEST_METHOD']==='POST'&&checkCsrf($_POST['csrf']??null)){$st=$pdo->prepare('INSERT INTO pengajuan_absensi(sesi_id,mahasiswa_id,jenis,alasan) SELECT ?,m.id,?,? FROM mahasiswa m JOIN users u ON u.username=m.npm WHERE u.id=?');$st->execute([(int)$_POST['sesi_id'],$_POST['jenis'],trim($_POST['alasan']),userId()]);$msg='Pengajuan dikirim.';} $st=$pdo->query("SELECT s.id,s.tanggal,mk.nama_mk FROM sesi_absensi s JOIN jadwal j ON j.id=s.jadwal_id JOIN mata_kuliah mk ON mk.id=j.mata_kuliah_id WHERE s.tanggal>=CURDATE() ORDER BY s.tanggal LIMIT 20");$sessions=$st->fetchAll(); ?><section class="section"><form method="post"><input type="hidden" name="csrf" value="<?=e(csrfToken())?>"><?php if($msg):?><div class="notice success"><?=e($msg)?></div><?php endif;?><div class="form-grid"><div class="input-group"><label>Sesi</label><select class="select" name="sesi_id" required><?php foreach($sessions as $s):?><option value="<?=$s['id']?>"><?=e($s['tanggal'].' · '.$s['nama_mk'])?></option><?php endforeach;?></select></div><div class="input-group"><label>Jenis</label><select class="select" name="jenis"><option>Izin</option><option>Sakit</option></select></div><div class="input-group full"><label>Alasan</label><textarea class="input" name="alasan" rows="6" required></textarea></div></div><button class="btn btn-primary">Kirim Pengajuan</button></form></section>
</main></div><?php require __DIR__.'/../partials/footer.php'; ?>